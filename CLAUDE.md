# Capacity Planner — Codebase Notes

## Architecture
Pure HTML/CSS/JS (no framework) + Supabase (auth + storage). Entry: `index.html`
→ `dist/app.*.min.js`, built by `node build.js` (IIFE concat + terser). Cross-module
wiring is `window.X` globals, not imports — see ADR-0003. Facts live in generated
docs (derived from source); this file holds only the reading path + working rules.

## Reading path (fresh session)
1. **`docs/architecture/generated/SYSTEM_MAP.md`** — module table, build order,
   migration ordering, notification emit/listen map. (Generated; the real entry point.)
2. **`docs/architecture/generated/REGISTRY.md`** — stores/enums/ID patterns/counts.
3. **`docs/architecture/generated/SCHEMA_REFERENCE.md`** — per-store fields + notes.
4. `docs/architecture/knowledge/` — GEOMETRY (invariants), DESIGN_SYSTEM
   (presentation rules), PHILOSOPHY (judgment), `annotations/*.yaml`, STATE.
5. `docs/architecture/adr/` — Architecture Decision Records (0001..0010).
6. `docs/architecture/AGENT_NOTES.md` — operational detail that doesn't fit above.

> `generated/` is an artifact — never hand-edit. Change `knowledge/` or source
> docblocks and re-run docgen. The diff gate fails on hand-edits.

## Capture protocol (do this in the same edit as the code change)
- Add an **export** (`window.X = …`) → add a one-line `// @owns X — <what>` docblock.
- Add a **non-obvious branch** or deliberate weirdness → `// @intent <why>`.
- Add a **decision** → write the ADR; reference it `@see ADR-000N`.
- Add a **deprecation / field lineage** → note it in `schema.yaml`.
- Add an **invariant** → note it in `knowledge/GEOMETRY.md`.
- Add a **transient note** → `STATE.md` with a promote-by date.
- Add/rename a **CSS token** → define it in `styles.css :root` per `DESIGN_SYSTEM.md`.
- Then run: `npm run docs:generate && npm run docs:check` (must pass before merge).
The four gates enforce: every export has `@owns`; every store is annotated; no
orphan notes; `generated/` matches source; no undefined CSS token (css-check,
also run by the build).

## Hierarchy
Priority Level → Focus → Sub-Focus → Epic → Story.

## Strangler-fig rule
Any feature touching `js/app.js` must first extract one responsibility (functions
sharing a store, describable in one sentence without "and").

## Commands
- Build: `npm run build` → `dist/`
- Docs: `npm run docs:generate` · `npm run docs:check`
- Tests: `npx playwright test --reporter=line` (Chromium; port 8080). Auth seeded
  from `SUPABASE_AUTH_STATE` in `.env` via `tests/global-setup.ts`. Details: AGENT_NOTES.md.

## Maintenance protocol (last step of every task)
- After a change, ensure `npm run docs:generate && npm run docs:check` pass.
- Keep this file ≤ ~70 lines; move overflow to `AGENT_NOTES.md`.
- Version line (update every change):
`Last updated: 2026-07-28 after Task Strategic-Layer (phases 0-4 data layer) — ADR-0011 epicWrites: second write spine (storyWrites was the first); canTransitionStatus had ONE caller hardcoded to 'story', so EPIC/FOCUS/SPRINT_TRANSITIONS were dead code and epic writes ran through three unguarded DB.put paths. EPIC_STATUS.CANDIDATE is a status, not a store (15 call sites traced, 5 needed changing) — it works because backlogView:942 already skips zero-story epics and no capacity read looks at epic status. The gate is a SECOND predicate (canPromoteEpic) because canTransitionStatus takes no record; it is checked against the epic MERGED with pending updates, since the panel saves the last business-case field and the status in one action. Rank is derived, never stored; wsjfScore returns null not 0 for partial inputs (unscored != worthless). migrateEpicsToStructuredScoring un-flattens the WSJF/size/problem/outcome text mergeImport wrote into epic.vision — and deriving the score FIXES real data (candidate_02.md says WSJF 25 for (8+9+7)/1 = 24). ADR-0012 cycles: membership DERIVED from date overlap, no cycleId anywhere. Overlap not containment — Jun 11 2026 is a Thursday and sprints snap to Monday, so containment drops the first AND last sprint of every cycle. Cycles never overlap (validateLocationPeriod's loop reused, shared boundary = handover); gaps derive to null and are legitimate. FROZEN at close via closedSnapshot, because re-dating a closed cycle silently rewrites its history. strategyWrites owns its own cache + hydrate() so the SYNCHRONOUS calendarView.render can read it — js/app.js diff is ZERO lines. NOTE deriveSprintDateRange returns {endDate,...} and NOT startDate. ADR-0013: 12 rituals -> 6 steps + 5 views. Evidence is the real first pass — every content-creating artifact was filled, all nine AGGREGATING ones abandoned (admin/03 themes/ deleted outright; focus thesis marked committed with THEMES still [placeholder]). theme.memberIdeas[] + theme.subFocusId remove two hand copy-pastes; focus end-state 3 bullets -> 1 line (3 produced 1 incomplete answer). Sequencing proposes; approval writes epic.plannedSprintId, never story.sprintId (a promoted epic has no stories yet). Also: notificationRegistry emit() was DROPPING the first payload-less emit of every turn (_pending?.add ran before _scheduleFlush allocated the Set — live defect from 41fe622); attachments generalised story-only -> any entity (storage key needed no type segment, ids are already unique) and twoStepConfirm moved to utils (the duplicate-decl build gate forbade a second copy). scripts/parseCycle.mjs seeds the real cycle: 5 themes + 4 admin candidates + 25/25/25/15/10 targets, candidate_05 blank skipped. Phase 4 UI shipped: cycle band on the calendar (models _renderPeriodBands, no per-cycle colour per DESIGN_SYSTEM rule 2), cycle as the detail panel's 6th type, cycle rail in the Backlog's previously-unused companion slot, css/strategy.css (7th CSS file, zero new tokens). Phase 5 shipped: strangler-fig cut #4 extracted generateAnalytics -> js/analyticsView.js (verified a PURE move, 79 logical lines identical modulo this.data->app.data) which paid for the Strategy tab's one switchTab branch; app.js 1312 -> 1227. strategyView hosts Ritual 1.2 (portfolio from LIVE epic counts + trailing 6-sprint allocation), 2.3/3.1 (candidate pool, rank derived, cut line DERIVED from the focus's capacity target) and 3.2 (committed vs deriveCapacityForDateRange). Browser-verified. Two build-gate catches worth remembering: `render`/`_rerenderIfVisible` collide across views (renderStrategy, per todayView's renderToday), and hydrate() RESOLVES with null on failure rather than rejecting, so a .catch never fires — branch on the resolved value or the surface spins on 'Loading...'. cycle-1 ingestion shipped: dataPortability.importCycle seeds cycle + focus theses + focus.themes[] from parseCycle output through the SAME Inbox picker (dispatches on version), idempotent by (name,startDate), themes dedup by normalized name within the focus. Candidates deliberately NOT imported there — mergeImport('candidates-1') already owns that path. Spec audit closed six documented-but-uncoded fields: themeId (+ epic panel theme picker, options scoped to the epic's own focus), killReason (shown only while archived — the spec kills WITH rationale), generationSource (was parsed by neither parseCandidates nor carried by mergeImport; the GENERATION_SOURCE enum was entirely dead), and plannedSprintId re-labelled RESERVED rather than pretending it exists. Added the two ADR-0013 live views that were promised and missing: theme portfolio with the 2-4 balance check (Ritual 2.1) and the candidate distribution check vs capacity targets (2.3). The Inbox picker now takes RAW .md candidate files (multi-select) — js/candidateParse.mjs holds the deterministic template parse and BOTH scripts/parseCandidates.mjs and the browser import it, so there is one template parser, not two. The offline-script-only design was premised on the parse needing an API key; it never did (only Notes->stories was LLM-assisted, and fallbackStories is a deterministic bullet split). Mixed selections run mergeImport once per focus, since its contract is one focus per payload. Sessions + outcome funnel shipped WITHOUT a closed cycle (user's call): strategyModel.deriveSessionFunnel is PURE and derives captured->scored->promoted->shipped LIVE from current epic state, scoped to the cycle's active focuses and to pipeline epics only (status candidate, or carrying wsjf/businessCase) so hand-made epics don't inflate it; 10 node assertions incl. the two exclusions. A `full` session auto-creates per cycle (no start ceremony — that would be filler; ADR-0013), lazily back-filled on view for pre-existing cycles; startRecut adds a kind:'recut' child. strategyView gained the live funnel, session history (one row per session, newest first, each with its funnel), and a Re-cut button. NOTE the naive dist import-leak grep false-positives on the word 'import' in UI strings; the real-statement check (from-clause / export const) is clean. All remaining spec items now built: sequencing propose→approve (strategyWrites.commitRoadmapSlot/approveRoadmap → epic.plannedSprintId, story creation prefills it), coherence panel (1.4), activation checklist as a derived 9-assertion commit gate (3.5, strategyWrites.stampCommitted freezes the ledger — distinct from CLOSING a cycle), focus classification enum + the max-5-active-strategic coherence flag (editable in the cycle panel), and theme.priorityWithinFocus (theme portfolio input). 17 node assertions on coherence/classification/activation; browser-verified the full 11-section tab. Two build-gate catches: `approve` collided with inboxView's top-level const (renamed locals _seqApprove etc., public keys unchanged), and the extraction of generateAnalytics->analyticsView (strangler-fig cut #4) was verified a PURE move. ONE deferred: importCycle auto-attaching source .md (parseCycle emits no raw text; manual attach covers it). Follow-up round (user-reported): the Strategy tab showed ONLY strategyWrites.current() (today's cycle), so any cycle whose dates don't cover today looked lost — added a cycle SWITCHER (_activeCycle: selection→current→most-recent) making the tab the hub for ALL cycles, plus New/Import/Capture/Close entries. Cycle NAME + DATES are now editable in the panel (were static text). Re-cut was a silent no-op — now captures a rankSnapshot and always toasts, visible in history. On-demand .md TEMPLATE FOLDER generation (js/strategyTemplates.mjs pure + js/strategyExport.js hand-rolled store-only ZIP, unzip-verified) matching parseCycle's structure so it round-trips. AUTO-SYNC of vault .md ↔ app values is NOT built: the user is on Firefox, which has no File System Access API, so a browser can't read/write a vault folder there at all — the honest paths are Chromium+FS-API or the existing Mini-side import_queue watcher pattern; presented as options, not built blind. Needs-review pass caught three more deviations from the user's stated needs: the focus panel never rendered a Files section though attachmentPanel supported 'focus' and schema.yaml called focus.attachments 'where the brain dump lands' — so brain dumps had no in-app container (FIXED: Files section + a themes list on the focus panel); strategyModel.RITUALS was defined and ADR-0013 claimed strategyView rendered the ritual sequence but nothing read it (FIXED: a six-step strip labelled Obsidian/computed/in-app + the dissolved-rituals line); importCycle does NOT attach the source .md files (parseCycle emits JSON without raw text, so auto-attach needs the parser to carry contents — DEFERRED; manual attach on the focus/cycle panels covers 'md as app-native values'). SQL migrations/20260728_strategic_layer.sql NOT YET APPLIED (nor is 20260719_import_queue.sql). Playwright still unrunnable (auth seed expired) — verification is node-level tests on the pure modules + parser runs against the real corpus.`
